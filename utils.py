def compute_positions(accel_x, accel_y):
    """
    Given acceleration data accel_x and accel_y,
    compute the relative positions of the "particle"
    >>> accel_x = [1, 2, 3]
    >>> accel_y
    """
